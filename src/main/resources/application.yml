server:
  port: 8081
springdoc:
  swagger-ui:
    path: swagger
spring:
  jpa:
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:file:~/fakedatadb
    driverClassName: org.h2.Driver
    username: user
    password: root